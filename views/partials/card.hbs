<div class="card">
  <img src="{{city.image}}" class="card-img-top" alt="{{city.name}}">
  <div class="card-body">
    <h5 class="card-title">{{city.name}}</h5>
    <a class="btn btn-primary" href="/cities/{{city._id}}">See details</a>
  </div>
</div>